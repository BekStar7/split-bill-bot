services:
  bot:
    build: .
    container_name: splitcheck-bot
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/splitcheck.db
    volumes:
      - ./data:/app/data
